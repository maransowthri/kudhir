import React from "react";

import Button from "components/UI/Button/Button";

const OnlineBanking = (props) => {
  return (
    <div>
      <Button type="primary" size="block" action={() => {}}>
        Donate Us
      </Button>
    </div>
  );
};

export default OnlineBanking;
